configMapGenerator:
  - name: mapper-config-importer-configmap
    namespace: healthworkforce-component
    files:
      - ./volume/openhim-import.json
      - ./volume/openhimConfig.js
resources:
  - ./mapper-config-importer-job.yaml
