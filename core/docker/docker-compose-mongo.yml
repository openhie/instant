version: '3.3'

services:
  mongo:
    image: mongo:4.2
    container_name: mongo
    volumes:
      - 'openhim-mongo:/data/db'
    command: ['--wiredTigerCacheSizeGB', '0.5']

volumes:
  openhim-mongo:
