apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: instant-openhie
  region: eu-west-2
  version: '1.14'

nodeGroups:
  - name: instant-openhie-workers
    instanceType: t3.medium
    desiredCapacity: 1
    minSize: 1
    maxSize: 3
