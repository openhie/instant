# OpenHIM Console Component

configMapGenerator:
  - name: core-openhim-console-configmap
    namespace: core-package
    files:
      - ./volume/openhim-console/default.json

resources:
  # deployment
  - ./openhim-console-deployment.yaml
  # service
  - ./openhim-console-service.yaml
